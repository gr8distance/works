= form_for @staff do |f|
  - if @staff.errors.any?
    #error_explanation
      %h2= "#{pluralize(@staff.errors.count, "error")} prohibited this staff from being saved:"
      %ul
        - @staff.errors.full_messages.each do |msg|
          %li= msg

  .input-field
    = f.label :name
    = f.text_field :name
  .input-field
    = f.label :staff_code
    = f.text_field :staff_code
  .input-field
    = f.select :user_id,@users
    = f.label :user_id
  .actions
    %button.btn.waves-effect.waves-light.blue スタッフ登録
